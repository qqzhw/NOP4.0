@model LoginModel
@using System.Net
@using Nop.Core
@inject IWebHelper webHelper
@{
    Layout = null;

    //title
    Html.AddTitleParts(T("PageTitle.Login").Text);
    //page class
    Html.AppendPageCssClassParts("html-login-page");

    //register URL with return URL (if specified)
    var registerUrl = Url.RouteUrl("Register");
    if (!String.IsNullOrEmpty(this.Context.Request.Query["returnUrl"]))
    {
        registerUrl = webHelper.ModifyQueryString(registerUrl, "returnurl=" + WebUtility.UrlEncode(this.Context.Request.Query["returnUrl"]), null);
    }
}
<!DOCTYPE html>

 
 
                @*<form asp-route="Login" asp-route-returnurl="@Context.Request.Query["ReturnUrl"]" method="post">
                    <div asp-validation-summary="ModelOnly" class="message-error">登录失败!</div>
                    <div class="title">
                        <strong></strong>
                    </div>
                    <div class="form-fields">
                        @if (Model.UsernamesEnabled)
                        {
                            <div class="inputs">
                                <label asp-for="Username">用户名:</label>
                                <input asp-for="Username" class="username" autofocus="autofocus" />
                                <span asp-validation-for="Username"></span>
                            </div>
                        }
                        else
                        {
                            <div class="inputs">
                                <label asp-for="Email">用户名:</label>
                                <input asp-for="Email" class="email" autofocus="autofocus" />
                                <span asp-validation-for="Email"></span>
                            </div>
                        }
                        <div class="inputs">
                            <label asp-for="Password">密码:</label>
                            <input asp-for="Password" class="password" />
                            <span asp-validation-for="Password"></span>
                        </div>
                        <div class="inputs reversed">
                            <input asp-for="RememberMe" />
                            <label asp-for="RememberMe">记住我</label>
                            <span class="forgot-password">
                                <a asp-route="PasswordRecovery">忘记密码</a>
                            </span>
                        </div>
                         
                    </div>
                    <div class="buttons">
                        <input class="button-1 login-button" type="submit" value="登录" />
                    </div>
                </form>*@
         

<html lang="en"> 
<head>
	<meta charset="utf-8" />
	<title>上位机管理</title>
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta content="width=device-width, initial-scale=1" name="viewport" /> 
	<meta content="" name="author" />
	 
	<link href="~/lib/fontAwesome/css/font-awesome-4.6.3.min.css" rel="stylesheet" type="text/css" />
	 
	<link href="~/lib/bootstrap/css/bootstrap.min.css" rel="stylesheet" type="text/css" />
 
	<link href="~/css/login.min.css" rel="stylesheet" type="text/css" />	 
 
	<link rel="shortcut icon" href="favicon.ico" />
	<style>
		.alert {
			border-width: 1px;
		} 
		.alert, .thumbnail {
			margin-bottom: 20px;
		}		 
	  .close {
			display: inline-block;
			margin-top: 0;
			margin-right: 0;
			width: 9px;
			height: 9px;
			background-repeat: no-repeat !important;
			text-indent: -10000px;
			outline: 0;
			background-image: url(../images/remove-icon-small.png) !important;
		}  
		.alert .alert-link, .close {
			font-weight: 700;
		}
	</style>
</head> 
<body class=" login"> 
	<div class="logo">
	 
	</div>
	 
	<div class="content">
		 
		<form asp-route="Login" asp-route-returnurl="@Context.Request.Query["ReturnUrl"]" method="post">
			<h3 class="form-title font-green">登录</h3>
			<div asp-validation-summary="ModelOnly" class="alert alert-danger display-hide">	  
					<a class="close" data-close="alert"></a>
					<span>	登录失败!   </span>				 
			</div>
			<div class="form-group">
			 
				<label class="control-label visible-ie8 visible-ie9">用户名</label>
				 
					<input asp-for="Email" type="text" class="form-control form-control-solid placeholder-no-fix" autocomplete="off" placeholder="请输入用户名" name="email" autofocus="autofocus" />
					<span asp-validation-for="Email"></span>
				 
			</div>
			<div class="form-group">
				<label class="control-label visible-ie8 visible-ie9">密码</label>
				<input asp-for="Password" class="form-control form-control-solid placeholder-no-fix" type="password" autocomplete="off" placeholder="请输入密码" name="password" />				 
				<span asp-validation-for="Password"></span>
			</div>
			<div class="form-actions">
				<input type="submit"style="background-color:#26a1ab;width:100px;color:white;float:right" class="btn green uppercase button-1 login-button" value="登录"/>
				 
				 <label class="rememberme check mt-checkbox mt-checkbox-outline">
					<input type="checkbox" asp-for="RememberMe" name="remember"  />
			    	<label asp-for="RememberMe">记住我</label>
				</label>
				@*<a href="javascript:;" id="forget-password" class="forget-password">Forgot Password?</a>*@
			</div>

			<div class="create-account">
				<p>

				</p>
			</div>
		</form>
		 
	</div>
	<div class="copyright"> 2017 © HCDZ. </div>
	 
	<script src="~/lib/jquery-1.10.2.min.js" type="text/javascript"></script>
	<script src="~/lib/bootstrap/bootstrap.min.js" type="text/javascript"></script>
	 
	<script src="~/lib/jquery-ui-1.10.3.custom.min.js" type="text/javascript"></script>
 
</body>

</html>